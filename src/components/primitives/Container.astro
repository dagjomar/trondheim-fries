---
interface Props {
	as?: keyof HTMLElementTagNameMap;
	class?: string;
	id?: string;
}

const { as = 'div', class: className = '', id } = Astro.props;
const Tag = as;
---

<Tag class={`container ${className}`.trim()} id={id}>
	<slot />
</Tag>
